pipeline {
    agent {
        docker { image 'praqma/native-gradle' }
    }
    stages {
        stage('Build') {
            steps {
                sh "make clean"
                sh "make all"
            }
        }
        stage('Results') {
            steps {
                junit 'out/bin/results_junit.xml'
                archiveArtifacts 'out/bin/main'
            }
        }
    }
}
